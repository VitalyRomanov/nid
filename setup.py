from distutils.core import setup

requirements = [
]


setup(name='nid',  # nid
      version='0.0.1',
      py_modules=['nid'],
      install_requires=requirements,
      extras_require={
      },
      dependency_links=[],
      scripts=[
      ],
      )